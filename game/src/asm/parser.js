import {Enum, TOKEN_TYPES, DIRECTIVES, OP_CODES, REGISTERS, token} from "./token";

class parser {
    constructor(input) {
	this.input = input;
	this.symbol_table = {};
	
    }
    
    symbol_search() {

    }

    symbol_insert() {
	
    }
}

export default parser;
